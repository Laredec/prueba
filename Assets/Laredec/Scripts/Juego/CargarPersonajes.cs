using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CargarPersonajes : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
        //GameManager.Instance.prefabsPersonajes = new GameObject[2];
        //GameManager.Instance.prefabsPersonajes[0] = Resources.Load("BigBox/Prefabs/Criaturas/CriaturasNuevas/Akiles") as GameObject;
        //GameManager.Instance.prefabsPersonajes[1] = Resources.Load("BigBox/Prefabs/Criaturas/CriaturasNuevas/Niño") as GameObject;
    }

    // Update is called once per frame
    void Update () {
		
	}
}
